@using maze_runner.game

@foreach (var sprite in Sprites)
{
    <img @ref="sprite.Ref" src="@sprite.Source" @onload="sprite.Loaded" />
}

@code {
    [Parameter]
    public List<Sprite> Sprites { get; set; }
}